{% extends "layoutadmin.html.twig" %}
{% block welcome %}
  <a style="color:#ffffff">Bienvenue
    {{ app.user.username }}
    <i class="fas fa-child"></i>
  </a>
{% endblock %}
{% block dashboard %}
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-12">
        <div class="card p-30 bg-success">
          <div class="media">
            <div class="media-left meida media-middle">
              <span>
                <i class="fas fa-tree f-s-40 color-secondary"></i>
              </span>
            </div>
            <div class="media-body media-text-right">
              <h2 class="text-white">{{ countparks }}</h2>
              <p class="m-b-0 text-white">Total de parc(s) actif</p>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="card">
      <div class="card-title">
        <h4>Liste des parcs</h4>
      </div>
      <div class="card-body">
        <div class="table-responsive">
          <table class="table">
            <thead>
              <tr>
                <th>ID</th>
                <th>Nom</th>
                <th>Description</th>
                <th>Localisation</th>
                <th>Prix</th>
                <th>Auteur</th>
                <th>Action</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                {% for park in parks %}
                <th scope="row">{{ park.id }}</th>
                <td>{{ park.title }}</td>
                <td>{{ park.description }}</td>
                <td>{{ park.place }}</td>
                <td>{{ park.price }}</td>
                <td>{{ park.user }}</td>
                <td>
                  <a class="btn btn-sm btn-primary" href="">
                    <i class="fas fa-eye"></i> Voir
                  </a>
                  <a class="btn btn-sm btn-warning" href="{{ path('edit_event_admin', { 'id': park.id}) }}">
                    <i class="fas fa-pencil-alt"></i> Editer
                  </a>
                  <a class="btn btn-sm btn-danger" href="">
                    <i class="fas fa-trash"></i> Supprimer
                  </a>
                </td>
              </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
{% endblock %}
