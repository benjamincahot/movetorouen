{% extends 'base.html.twig' %}
 {% block content %}
       <!-- Main jumbotron for a primary marketing message or call to action -->
    <div class="jumbotron">
        <div class="container">
          
            <h2>Liste des évènements</h2>

            <a href="{{ path('evenement_add') }}">Add event</a>
            <a href="{{ path('evenement_retrieve') }}">Retrieve event</a>
            <a href="{{ path('evenement_update') }}">Update event</a>
            <a href="{{ path('evenement_delete') }}">Delete event</a>
        </div><!-- /container -->

     </div> 
 {% endblock %}